CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(Preprocess)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE (${VTK_USE_FILE})
ADD_EXECUTABLE(Sobel-EdgeDetection Sobel-EdgeDetection.cpp)
TARGET_LINK_LIBRARIES(Sobel-EdgeDetection ${VTK_LIBRARIES})